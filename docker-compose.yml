version: '3.8'

services:
  crom-vision:
    build: .
    container_name: crom-vision-app
    restart: unless-stopped
    ports:
      - "${PORT:-8085}:${PORT:-8085}"
    volumes:
      - ./data:/app/data
    env_file:
      - .env

